@extends('layouts.app')
@section('title','خصائص المتغيرات')
@section('content')
<div class="page-header"><div><h1 class="page-heading">خصائص المتغيرات</h1><p class="page-subheading">خصائص إضافية غير المقاس واللون مثل الخامة، الموسم أو الموديل.</p></div></div>
@include('admin.catalog._nav')
<div class="card" style="margin-bottom:1rem"><div class="card-body"><form method="POST" action="{{ route('catalog.attributes.store') }}">@csrf<div style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:.75rem"><input name="code" class="form-input" placeholder="material" required><input name="name" class="form-input" placeholder="Material" required><input name="name_ar" class="form-input" placeholder="الخامة"><input name="sort_order" type="number" value="0" min="0" class="form-input"></div><button class="btn btn-gold" style="margin-top:.75rem">إضافة خاصية</button></form></div></div>
<div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem">@foreach($attributes as $attribute)<div class="card"><div class="card-header"><span class="card-title">{{ $attribute->displayName() }} <code>{{ $attribute->code }}</code></span><span class="badge {{ $attribute->is_active?'badge-active':'badge-inactive' }}">{{ $attribute->is_active?'مفعلة':'معطلة' }}</span></div><div class="card-body"><div style="display:flex;gap:.4rem;flex-wrap:wrap;margin-bottom:.75rem">@forelse($attribute->values as $value)<span class="badge {{ $value->is_active?'badge-active':'badge-inactive' }}">{{ $value->displayName() }}</span>@empty<span style="color:var(--text-muted)">لا توجد قيم.</span>@endforelse</div><form method="POST" action="{{ route('catalog.attributes.values.store',$attribute) }}">@csrf<div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:.5rem"><input name="code" class="form-input" placeholder="cotton" required><input name="name" class="form-input" placeholder="Cotton" required><input name="name_ar" class="form-input" placeholder="قطن"></div><button class="btn btn-outline btn-sm" style="margin-top:.5rem">إضافة قيمة</button></form></div></div>@endforeach</div>
@endsection
